import mmMessages from './messages/my.json';

const messages = {
  'my': mmMessages, // Language code (my) should match the code in constants.js. And the language code should match exactly with standard language codes. For myanmar, there is not region code.
};

export default [messages];
